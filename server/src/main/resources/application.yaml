spring:
  application:
    name: BoardBuddies-Server
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password:
  h2:
    console:
      enabled: true  # 웹 콘솔 열기
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
